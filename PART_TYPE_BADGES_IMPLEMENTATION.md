# Реалізація Part Type Badges

## Огляд

Додана функціональність для відображення бейджів типів деталей (part_type) на картках послуг. Система автоматично розбирає рядок типів деталей на окремі бейджи з унікальними кольорами.

## Створені/Оновлені файли

### 1. **Нові файли**

#### `/lib/part-type-badge-utils.ts`
- Утиліта для обробки part_type
- Конфіг кольорів для кожного типу деталі
- Функції для парсингу та отримання конфігів

**Типи деталей та їх кольори:**
- `original` → 🟨 Жовтий (bg-yellow-400, text-gray-900)
- `oled` → 🔴 Червоний (bg-red-500, text-white)
- `ips` → 🟢 Зелений (bg-green-500, text-white)
- `lcd` → 🟠 Помаранч (bg-orange-500, text-white)
- `premium` → 🔵 Синій (bg-blue-500, text-white)
- `refurbished` → 🟦 Сірий (bg-gray-500, text-white)

#### `/components/part-type-badges.tsx`
- React компонент для відображення бейджів
- Підтримує кілька типів деталей в одному рядку
- Можливість змінення контейнера та індивідуального стилю бейджа

### 2. **Оновлені файли**

#### `/app/api/models/[slug]/route.ts`
- Додано `part_type` до SELECT запиту
- `part_type` додано в об'єкт кожної послуги

#### `/app/[locale]/models/[slug]/model-page-client.tsx`
- Додано імпорт `PartTypeBadges` компонента
- Додано `part_type: string | null` до типу послуги
- Додано відображення бейджів у верхньому лівому куту карточки послуги
- Бейджи мають вертикальне розташування при кількох значеннях

#### `/app/api/services/[slug]/route.ts`
- Додано `part_type` до SELECT запиту
- `part_type` додано в об'єкт modelServiceData
- `part_type` додано в результат API

#### `/app/[locale]/services/[slug]/service-page-client.tsx`
- Додано імпорт `PartTypeBadges` компонента
- Додано `part_type?: string | null` до типу ServiceData
- Додано відображення бейджів у верхньому лівому куту блока з зображенням

## Як це працює

### Парсинг даних

Рядок `part_type` зберігається в БД у форматі, розділеній комами:
```
"original,oled"
"premium"
"lcd,refurbished"
```

### Компонент PartTypeBadges

Компонент автоматично:
1. Розбирає рядок на окремі типи
2. Отримує конфіг кольорів для кожного типу
3. Відображає бейджи з унікальними кольорами та стилями

### Розташування

**На сторінці моделей (карточки послуг):**
- Верхній лівий кут блока з зображенням
- Вертикальний стек при кількох бейджах

**На сторінці послуг:**
- Верхній лівий кут блока з зображенням
- Вертикальний стек при кількох бейджах

## Локалізація

Компонент автоматично шукає переклади за ключами:
```
Common.partType.original
Common.partType.oled
Common.partType.ips
Common.partType.lcd
Common.partType.premium
Common.partType.refurbished
```

Якщо перекладу немає, використовується текст з конфіга.

## Приклади використання

### Просто відображення
```tsx
<PartTypeBadges partTypeString="original,oled" />
```

### З кастомним стилем контейнера
```tsx
<PartTypeBadges 
  partTypeString="premium" 
  containerClassName="flex-col gap-1" 
/>
```

### З кастомним стилем бейджа
```tsx
<PartTypeBadges 
  partTypeString="original" 
  className="text-sm" 
/>
```

## Тестування

1. Перейти на сторінку будь-якої моделі
2. Якщо послуга має `part_type` - бейджи мають відображатися у верхньому лівому куті
3. При кількох типах деталей - вертикальне розташування
4. Кожен бейдж має унікальний колір

## Майбутні покращення

1. Додати локалізовані переклади для типів деталей
2. Дозволити адміністратору налаштовувати кольори типів
3. Додати фільтрацію послуг за part_type
4. Додати統計 по популярним типам деталей
